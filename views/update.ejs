<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Edit Flashcard</title>
</head>
<body>
    <div id="app">
        <h1>Edit Flashcard</h1>
        <div class="form-container">
            <form action="/api/flashcards/update/<%= card.id %>" method="POST">
                <div class="form-group" >
                    <div id="textarea">
                        <label for="front">Front:</label>
                        <textarea class="input-large" name="front" ><%= card.front %></textarea>
                    </div>
                    <div id="textarea">
                        <label for="back">Back:</label>
                        <textarea  class="input-large" name="back" ><%= card.back %></textarea>
                    </div>   
                   
                </div>
                <div id="navigation">
                    <button type="submit">Update</button>
                    <a href="/api/flashcards/allFlashcards" class="button">Cancel</a>
                </div>
            </form>
            
        </div>
    </div>
    <%-include('partials/footer')%>
</body>
</html>